symbol: GBPJPY

# Session boundaries (UTC hours, half-open [start, end)).
# GBPJPY is a cross pair: GBP (London) + JPY (Tokyo/Asian).
# Asian and London are the primary directional movers. London/NY overlap
# is typically quieter for a JPY cross. NY is the quietest window.
sessions:
  overnight: [21,  0]  # 21–00 UTC — post-NY dead time
  asian:     [0,   7]  # 00–07 UTC — Tokyo active, JPY-driven
  london:    [7,  12]  # 07–12 UTC — primary GBP session + Asian/London overlap
  london_ny: [12, 17]  # 12–17 UTC — lower energy for JPY cross
  ny:        [17, 21]  # 17–21 UTC — typically quiet

data:
  root: big-data/mlData/raw
  file_pattern: "{symbol}-sorted-{timeframe}.parquet"

timeframes:
  execution: 1min

mechanics:
  # precomputed in features.py — change these only if regenerating features
  ema_touch: 20           # EMA period used to compute ema20 column
  ema_traj: 3             # EMA period used to compute ema3 column
  rsi_period: 30          # RSI period used to compute rsi30 column
  context_stable_bars: 3  # consecutive bars for context ST stability flag
  # live config — read by engine/strategy at runtime
  of_depth_a2: 4          # pivot lookback depth for A2 OF gate
  of_min_a2: 2            # minimum pivots required for A2 OF gate
  pivot_maxlen: 8         # max pivots stored in pivot array

hypotheses:
  A1:
    enabled: true
    trigger_mode: ema20_touch   # touch EMA20 + clean_approach + bounce
    ema3_slope_mode: lax        # lax: ema3[n-2] > ema3[n]; strict: full monotone chain
    sl_mode: ema20_st_pocket    # pocket-width clamped to [a1_sl_min, a1_sl_max]
    a1_sl_min: 0.15             # 15 pip floor (GBPJPY pip = 0.01)
    a1_sl_max: 0.25             # 25 pip cap
    rr: 1.5
    session_gate: false         # OFF in Pass 1; enabled via --session-gate in Pass 2
    allowed_sessions: [asian, london, london_ny, ny]  # drop overnight: ph3=-0.075R, ph4=-0.167R
    pullback_gate: true         # ON — core hypothesis constraint
    flicker_suppression: own_hypothesis
    rsi_gate_enabled: false
    rsi_bull_gate: 55.0
    rsi_bear_gate: 48.0
  A2:
    enabled: true
    session_gate: false         # OFF in Pass 1; enabled via --session-gate in Pass 2
    allowed_sessions: [london_ny, ny, overnight]  # phase3 only: asian/london negative; N=53 sparse
    pullback_gate: true         # ON — core hypothesis constraint
    flicker_suppression: own_hypothesis
    of_direction_mode: lax      # lax = trust context TF direction
    pd_level: 0.51              # pullback depth to enter: 51%
    sl_min: 0.15                # 15 pip floor (GBPJPY pip = 0.01)
    sl_max: 0.25                # 25 pip cap
    sl_epsilon: 0.03            # 3 pip buffer beyond last pivot extreme
    rr: 1.5
  B:
    enabled: true
    session_gate: false         # OFF in Pass 1; enabled via --session-gate in Pass 2
    allowed_sessions: [asian, london, london_ny, overnight]  # drop ny: ph3=-0.149R
    pullback_gate: false
    flicker_suppression: own_hypothesis
    sl_fixed: 0.25              # 25 pip fixed SL (GBPJPY pip = 0.01)
    rr: 2.0

execution:
  policy: policy3
  require_context_stable: true
  cooldown_enabled: false
  cooldown_bars: 6
  max_concurrent_positions_per_symbol: 1
  same_bar_priority: [B, A2, A1]
  policy3:
    allow_any_direction_stack: true
    allow_same_bar_overlap: false
  policy2:
    trigger_first: true

combos:
  A1 only: [A1]
  A2 only: [A2]
  B only: [B]
  A1+B: [A1, B]
  A2+B: [A2, B]
  A1+A2+B: [A1, A2, B]

cross_phase:
  enabled: true
  branches:
    A1:
      enabled: true
      phase: phase4   # strongest A1: ev=+0.056R, pf=1.097, dd=34.5R (gated)
    A2:
      enabled: true
      phase: phase3   # only viable A2: ev=+0.220R, pf=1.429, N=41 (gated)
    B:
      enabled: true
      phase: phase2   # strongest B: ev=+0.102R, pf=1.160, N=956, dd=23.0R (gated)
  same_bar_priority: [B, A2, A1]
  max_concurrent_positions_per_symbol: 1

architecture_phases:
  phase1:
    label: 5m ST(12,3) entry vs 1H ST(12,3) context
    entry_timeframe: 5m
    context_timeframe: 1h
    entry_st: {period: 12, multiplier: 3.0}
    context_st: {period: 12, multiplier: 3.0}
  phase2:
    label: 5m ST(12,3) entry vs 1H ST(11,2) context
    entry_timeframe: 5m
    context_timeframe: 1h
    entry_st: {period: 12, multiplier: 3.0}
    context_st: {period: 11, multiplier: 2.0}
  phase3:
    label: 15m ST(12,3) entry vs 1H ST(12,3) context
    entry_timeframe: 15m
    context_timeframe: 1h
    entry_st: {period: 12, multiplier: 3.0}
    context_st: {period: 12, multiplier: 3.0}
  phase4:
    label: 15m ST(11,2) entry vs 1H ST(12,3) context
    entry_timeframe: 15m
    context_timeframe: 1h
    entry_st: {period: 11, multiplier: 2.0}
    context_st: {period: 12, multiplier: 3.0}
  phase5:
    label: 15m ST(11,2) entry vs 1H ST(11,2) context
    entry_timeframe: 15m
    context_timeframe: 1h
    entry_st: {period: 11, multiplier: 2.0}
    context_st: {period: 11, multiplier: 2.0}
  phase6:
    label: 15m ST(12,3) entry vs 4H ST(12,3) context
    entry_timeframe: 15m
    context_timeframe: 4h
    entry_st: {period: 12, multiplier: 3.0}
    context_st: {period: 12, multiplier: 3.0}
  phase7:
    label: 15m ST(11,2) entry vs 4H ST(12,3) context
    entry_timeframe: 15m
    context_timeframe: 4h
    entry_st: {period: 11, multiplier: 2.0}
    context_st: {period: 12, multiplier: 3.0}
  phase8:
    label: 15m ST(11,2) entry vs 4H ST(11,2) context
    entry_timeframe: 15m
    context_timeframe: 4h
    entry_st: {period: 11, multiplier: 2.0}
    context_st: {period: 11, multiplier: 2.0}

deployment:
  # phase3 A1+A2+B gated — ev=+0.046R, pf=1.077, total=+132R, dd=65.0R
  # cross-phase (A1@ph4+A2@ph3+B@ph2) tested and discarded — tied, not worth complexity
  selected_phase: phase3
  selected_combo: A1+A2+B

regime_gate: {}
